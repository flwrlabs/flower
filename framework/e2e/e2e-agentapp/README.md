# AgentApp Flower testing

This directory is used for testing Flower running AgentApp, that is the capability of Flower to execute agent runs.

It uses a single LLM call with web search enabled to answer a prompt in one single sentence.
