# Generated by Flower (flowertune-llm)

[meta]
run_id = "{{ run_id }}"
round_id = "{{ round_id }}"
node_id = "{{ node_id }}"
client_name = "{{ client_name }}"

[model]
name = "{{ model_name }}"
flavor = "{{ model_flavor }}"
hf_assets_path = "{{ hf_assets_path }}"

[data]
dataset_name = "{{ dataset_name }}"
dataset_path = "{{ dataset_path }}"

[train]
steps = {{ train_steps }}

[io]
input_checkpoint = "{{ input_checkpoint_path }}"
output_checkpoint = "{{ output_checkpoint_path }}"
input_dcp_dir = "{{ input_dcp_dir }}"
output_dcp_dir = "{{ output_dcp_dir }}"
